appId: com.example.app
name: "SC-06: Login with invalid credentials shows error"
tags:
  - SCRUM-10
  - negative
  - high
---
# Precondition: Launch app fresh and navigate to login screen
- launchApp:
    clearState: true

# Navigate to the login screen
- tapOn: "Login"

# Enter an email that does not exist or has wrong password
- tapOn: "Email"
- inputText: "wronguser@example.com"

# Enter an incorrect password
- tapOn: "Password"
- inputText: "WrongPassword123"

# Tap the Login button
- tapOn: "Login"

# Verify an error message is displayed indicating invalid credentials
- assertVisible: "Invalid"

# Verify user remains on the login screen
- assertVisible: "Email"
- assertVisible: "Password"
