appId: com.example.app
name: "SC-03: Password field masks input with secure text entry"
tags:
  - SCRUM-10
  - positive
  - high
---
# Precondition: Launch app fresh and navigate to login screen
- launchApp:
    clearState: true

# Navigate to the login screen
- tapOn: "Login"

# Tap the password input field
- tapOn: "Password"

# Type a password
- inputText: "TestPassword123"

# Verify: The typed characters should NOT be visible as plain text
# The password text should be masked (dots/bullets), so the plain text should not appear on screen
- assertNotVisible: "TestPassword123"
